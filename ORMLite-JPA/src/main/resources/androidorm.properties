# A file with this name and this one property MUST exist at the root of your classpath

mainActivity=com.example.ormlite.SavingActivity

personClass=domain.jpa.PersonJPA
addressClass=domain.jpa.AddressJPA
